import { hot } from "react-hot-loader";
import "./App.css";

function App() {
  return (
    <div className="App">
      <h1>Hello, World!</h1>
    </div>
  );
}

// eslint-disable-next-line no-undef
export default hot(module)(App);
